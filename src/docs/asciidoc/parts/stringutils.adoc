== String Utilities

=== Converting objects to strings

Use the link:{groovydoc}++/StringUtils.html#stringize(java.lang.Object)++[stringize] method to convert nearly anything to a string or a collection of strings. Closures are evaluated and the results are then converted to strings.

[source,groovy]
----
include::{compatdir}/StringUtilsSpec.groovy[tags=stringize_string,indent=0]

include::{compatdir}/StringUtilsSpec.groovy[tags=stringize_collection,indent=0]
----